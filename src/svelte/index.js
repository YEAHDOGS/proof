/**
 * @file `@dogs/proof/svelte` — visual components (Svelte 5 peer required).
 *
 * Raw .svelte sources, compiled by the consumer's bundler (the standard
 * Svelte-library pattern). Data-only consumers never import this subpath
 * and need no Svelte install.
 */

export { default as Chart } from './Chart.svelte'
